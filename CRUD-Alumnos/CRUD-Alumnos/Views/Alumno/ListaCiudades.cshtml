@model IEnumerable<CRUD_Alumnos.Models.Ciudade>

    <select class="form-control" id="codCiudad" name="codCiudad">
        @foreach (var item in Model)
        {
            <option value = "@item.Id" > @item.Nombre </option>
        }
    </select>


